#ifndef MAIN_H
#define MAIN_H

#include <stdarg.h>
#include <stdlib.h>
#include <unistd.h>
void _sprintf(const char *str);
void _count(int *ptr, char *str);
int _printf(const char *format, ...);

#endif
